
  # Generate website from text

  This is a code bundle for Generate website from text. The original project is available at https://www.figma.com/design/c68SZR8q86xe8aszi5avWo/Generate-website-from-text.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  